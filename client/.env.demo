# Only loaded by `vite build --mode demo`. Enables DevSwitcher (the real-login
# role switcher) in an otherwise-production build, for the public demo/preview
# deployment. Delete this file (or just stop building with --mode demo) before
# a real production deployment to remove DevSwitcher from the shipped bundle.
VITE_ENABLE_DEV_SWITCHER=true
